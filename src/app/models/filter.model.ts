export interface Filter{
    brands: number[],
    models: number[],
    categories: number[],
    types: number[],
    collections: number[],
    colors: number[],
    sizes: number[],
    query:string
  }