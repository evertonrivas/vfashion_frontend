import { DevolutionStatusPipe } from './devolution-status.pipe';

describe('DevolutionStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DevolutionStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
