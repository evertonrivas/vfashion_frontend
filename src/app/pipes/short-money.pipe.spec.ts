import { ShortMoneyPipe } from './short-money.pipe';

describe('ShortMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
